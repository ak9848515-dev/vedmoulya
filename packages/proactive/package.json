{
  "name": "@vedmoulya/proactive",
  "version": "1.0.0",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "description": "Proactive Intelligence for VedMoulya — a COMPOSITION layer that turns existing Brain/scheduler/marketplace/execution intelligence into structured, evidence-only, authorization-aware recommendations. NOT an engine: no discovery/provider-selection/approval/execution/memory/learning authority lives here — the frozen estate owns those.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@vedmoulya/brain": "*",
    "@vedmoulya/core": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "*"
  }
}
