{
  "name": "@vedmoulya/voice",
  "version": "1.0.0",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "description": "Voice interaction foundation for VedMoulya — narrow speech capability seams (SpeechToTextPort / TextToSpeechPort), an owner-scoped bounded conversation store, and the deterministic VOICE ≠ AUTHORIZATION safety contract. NOT a voice engine: no intent/approval/budget/execution/memory logic lives here — the Brain remains the orchestrator.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@vedmoulya/brain": "*",
    "@vedmoulya/core": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "*"
  }
}
