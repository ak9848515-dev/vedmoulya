# README.md

# VedMoulya

## Mission

Empower every determined individual to build a sustainable livelihood through knowledge, execution, and intelligent technology.

## Vision

Build the world's most trusted Execution Operating System that helps people transform knowledge into sustainable livelihoods.

## Motto

Knowledge • Value • Execution

## Core Philosophy

Knowledge alone does not change lives.

Knowledge → Understanding → Decision → Execution → Value → Livelihood

## Long-Term Goal

Help one million people create sustainable livelihoods.

## Current Status

- **Phase:** 🟢 **OS v1.0 FROZEN** (2026-08-07, OS-003)
- **Mission:** 001 — Company Constitution (foundation) → 020 — Version 1.0 (released)
- **Version:** 1.0.0
- **Status:** 🟢 Production certified — [2026-07-31 report](./docs/PROJECT_REPORT_2026-07-31.md) · 🟢 **Enterprise Certified (CERT-002)** — [2026-08-06 report](./docs/CERT-002_Completion_Report.md) · 🟢 **Final OS Certification (OS-002)** — [2026-08-07 report](./09_Documents/OS-002_Final_Certification_Report.md) · 🟢 **OS v1.0 FROZEN (OS-003)** — [2026-08-07 report](./09_Documents/OS-003_V1_Release_Report.md) · 🟢 **AI-RUNTIME-002 Condition-Free Production Approved** — [2026-08-08 certification](./09_Documents/AI_RUNTIME_002_CONDITION_FREE_CERTIFICATION.md) · 🟢 **AI-RUNTIME-003 Condition-Free Production Approved** — [2026-08-08 report](./09_Documents/AI_RUNTIME_003_Completion_Report.md) · 🟢 **EPIC-006 Orchestrated AI / Loop Engine COMPLETE** — [2026-08-08 report](./09_Documents/EPIC_006_COMPLETION_REPORT.md) · 🟢 **EPIC-007 AI Application Factory COMPLETE** — [2026-08-08 report](./09_Documents/EPIC_007_COMPLETION_REPORT.md) · 🟢 **EPIC-008 Real Application Workspace & Production UX COMPLETE** — [2026-08-09 report](./09_Documents/EPIC_008_COMPLETION_REPORT.md) · 🟢 **EPIC-009 Product Intelligence & Requirements Engine COMPLETE** — [2026-08-09 report](./09_Documents/EPIC_009_COMPLETION_REPORT.md) · 🟢 **EPIC-010 Adaptive Application Experience & Visual Intelligence COMPLETE** — [2026-08-09 report](./09_Documents/EPIC_010_COMPLETION_REPORT.md) · 🟢 **EPIC-011 Production Validation & Autonomous Quality COMPLETE** — [2026-08-09 report](./09_Documents/EPIC_011_COMPLETION_REPORT.md) · 🟢 **EPIC-012 Production Observability, Control Plane & Operations COMPLETE** — [2026-08-10 report](./09_Documents/EPIC_012_COMPLETION_REPORT.md) · 🟢 **EPIC-012A Premium Experience Refinement + AI Provider Intelligence COMPLETE** — [2026-08-10 report](./09_Documents/EPIC_012A_COMPLETION_REPORT.md) · 🟢 **EPIC-012B AI Provider Intelligence & Model Discovery COMPLETE** — [2026-08-10 report](./09_Documents/EPIC_012B_COMPLETION_REPORT.md) · 🟢 **EPIC-012C AI World Discovery & Market Intelligence COMPLETE** — [2026-08-10 report](./09_Documents/EPIC_012C_COMPLETION_REPORT.md) · 🟢 **EPIC-013 AI Capability Marketplace & Factory Intelligence COMPLETE** — [2026-08-10 report](./09_Documents/EPIC_013_COMPLETION_REPORT.md) · 🟢 **EPIC-014 Capability Execution Engine COMPLETE** — [2026-08-10 report](./09_Documents/EPIC_014_COMPLETION_REPORT.md) · 🟢 **EPIC-016 VedMoulya Brain COMPLETE (Phase 0+1)** — [2026-08-11 report](./09_Documents/EPIC_016_COMPLETION_REPORT.md) · 🟢 **EPIC-015 VedMoulya Intelligence COMPLETE** — [2026-08-11 report](./09_Documents/EPIC_015_COMPLETION_REPORT.md) · 🟢 **EPIC-017 Live Intelligence Bridge COMPLETE** — [2026-08-11 report](./09_Documents/EPIC_017_COMPLETION_REPORT.md) · 🟢 **EPIC-018 AI World Discovery Scheduler & Discovery Engine COMPLETE** — [2026-08-12 report](./09_Documents/EPIC_018_SCHEDULER_COMPLETION_REPORT.md) · 🟢 **EPIC-018 Production Startup & Environment Reliability COMPLETE** — [2026-08-11 report](./09_Documents/EPIC_018_COMPLETION_REPORT.md) · 🟢 **EPIC-019 Platform Startup, Environment & Provider Runtime Hardening COMPLETE** — [2026-08-12 report](./09_Documents/EPIC_019_COMPLETION_REPORT.md) · 🟢 **EPIC-020 Continuous Intelligence & Adaptive Orchestration COMPLETE** — [2026-08-12 report](./09_Documents/EPIC_020_COMPLETION_REPORT.md) · 🟢 **EPIC-020 Outcome & Revenue Intelligence COMPLETE** — [2026-08-12 report](./09_Documents/EPIC_020_OUTCOME_COMPLETION_REPORT.md) · 🟢 **SPRINT-022 Persistent Intelligence Foundation VERIFIED** (durable owner-scoped Postgres behind every intelligence store — in-memory in dev, Postgres in production; restart-recovery verified against live PostgreSQL 4/4) — [2026-08-12 report](./04_Sprints/SPRINT-022_COMPLETION_REPORT.md) · 🟢 **SPRINT-024 Live Outcome Verification & Real-Runtime Execution COMPLETE** (REAL runtime artifact verification — root-confined ArtifactReaderPort + deterministic ArtifactVerifier composed into the existing StepVerifier; honest outcome verdicts, never fabricated success; 12 real-artifact runtime journeys 36/36 PASS, CI-wired) — [2026-08-12 report](./04_Sprints/SPRINT-024_COMPLETION_REPORT.md) · 🟢 **SPRINT-025 Continuous Learning, Outcome Memory & Adaptive Improvement COMPLETE** (evidence-driven learning from completed REAL journeys with zero new engines — LearningSignal FACT/INFERENCE/UNKNOWN model + verdict-gated BrainOutcomeMemory, UNKNOWN/FAILED never become SUCCESS; user-correction loop correctLearning, explicit user facts override inferred preferences; advisory experience tie-break in provider selection, quality-first preserved; /brain learning feed with "You told me / I observed / I inferred" labels + Correct/Forget; 15-journey learning benchmark 25/25 PASS, CI-wired) — [2026-08-12 report](./04_Sprints/SPRINT-025_COMPLETION_REPORT.md) · 🟢 **SPRINT-040 FOUNDER EVIDENCE LOOP + LOCAL RUNTIME VERIFICATION COMPLETE** (2026-08-16 — the first end-to-end operational path proven LIVE over the frozen estate, **NEW ENGINES CREATED: 0**: Docker runtime → register/login → founder observation → provenance validation → evidence persistence → scoring → customer discovery → next-best-action → verified-payment progression, founder = ultimate authority. Three genuine defects found + fixed minimally: **D1** the identity `users` table was the ONE Postgres store never created anywhere (DB init only opened a connection) → first-run auth failed `REGISTRATION_FAILED`; fixed with the estate-convention `PostgresIdentityRepository.ensureTable()` (idempotent `CREATE TABLE IF NOT EXISTS`, unique email/google_id indexes), wired fire-and-forget in `createProductionIdentityRepository()` and awaited in the web auth-app · **D2** `IDENTITY_DATABASE_URL` unset locally → added to gitignored `.env.local` (Docker dev creds; no API keys) · **D3** no email-verification delivery exists (no SMTP) while the domain blocks sign-in for unverified accounts → dev/test-only auto-verify in the EXISTING `AuthService.signUp` (production/staging unchanged). Live verification: sign-up 201 · duplicate 409 · validation 400 · sign-in 200 · session 200 · sign-out 200; provenance refusal; claimed VERIFIED downgraded; calibration refuses UNKNOWN fabrication; prospect bounded chain + invalid-jump refusal; verified-payment-only ladder REVENUE_VERIFIED → REPEAT_REVENUE → REPEATABLE_BUSINESS; explainable next-best-action; honest EMPTY datasets; suites world-model 298/298 · identity 283/283 · api 1010/1010 · web 220/220 · typecheck 0 · lint 0/0 · build PASS · benchmarks all PASS · coverage gate 2/2; Docker postgres+redis healthy, / + /login 200; honest: `vedmoulya-web` is NOT a container (not in compose — web runs via `next dev`); production email verification remains a documented pre-existing gap; only LOCAL TEST data used — nothing fabricated) — [2026-08-16 report](./04_Sprints/SPRINT-040_COMPLETION_REPORT.md) · 🟢 **SPRINT-039 FOUNDER EVIDENCE LOOP COMPLETE** (2026-08-15 — closed the last loop between the founder's REAL observations and the system's advisory scoring, **NEW ENGINES CREATED: 0**, over the frozen estate: **founder observations with MANDATORY provenance** (refused `PROVENANCE_REQUIRED` otherwise; sanitized at the boundary; explicit evidence states OBSERVED/REPORTED_BY_CUSTOMER/FOUNDER_OBSERVED/DOCUMENTED/VERIFIED/HYPOTHESIS/UNKNOWN/CONFLICTING; a claimed VERIFIED is downgraded — VERIFIED requires a real cross-check; HYPOTHESIS is the honest default) · **customer-discovery ledger** (NOT a CRM, no PII dumps; bounded status chain CONTACTED→…→VERIFIED_PAYMENT + LOST; discovery ≠ validation, interest ≠ revenue, WTP ≠ payment — ONLY a `verified_payment` record reaches `REVENUE_VERIFIED`) · **bounded evidence calibration** (`CALIBRATION_DELTA_MAX` 0.05 per event over the EXISTING SPRINT-038 factors; strength-scaled; UNKNOWN never zero; conflicts visible, never silently resolved) · **deterministic 8-dimension evidence quality** (honest UNKNOWN; stale evidence never inflates) · **explainable NEXT BEST ACTION** (TALK_TO_CUSTOMERS/TEST_WTP/REQUEST_PAYMENT/VERIFY_PROBLEM/RUN_NO_COST_EXPERIMENT/**STOP** — the system CAN say "do not build this") · **evidence-driven opportunity comparison** (STRONG_EVIDENCE/PROMISING/NEEDS_CUSTOMER_VALIDATION/INSUFFICIENT_EVIDENCE/STOP/UNKNOWN — a high score alone is never STRONG_EVIDENCE) · **Command Center drill-downs** (evidence/prospects/next action per opportunity; honest EMPTY copy) · **voice read-only presentation** (VOICE ≠ AUTHORIZATION preserved) · owner-scoped stores (in-memory + Postgres `world_observations`/`world_prospects`) · gateway _*world.* +10 procedures_* (auth + rate tier + IDOR + zod) · **`evidence:benchmark` 20/20 + `discovery:benchmark` 10/10** (benchmarks chain now 20 harnesses) + vitest gates. Verification 2026-08-15: world-model **298/298 (23 files)**, services/api **1010 (50 files)**, web **219/219 (22 files)**, typecheck **0**, lint **0 errors · 0 warnings**, `next build` **PASS**, benchmarks all PASS, coverage gate **world-model 91.11/82.18/90.83/94.34 · api PASS**. Honest: **EMPTY datasets** — NO fabricated observations/prospects/customers/revenue; real founder observation entry ready; live world signals + real provider execution remain **OPERATOR-REQUIRED**; no real customer/revenue evidence exists) — [2026-08-15 report](./04_Sprints/SPRINT-039_COMPLETION_REPORT.md) · 🟢 **SPRINT-038 OPPORTUNITY DISCOVERY & REVENUE VALIDATION COMPLETE** (2026-08-15 — VedMoulya became PRACTICAL, **NEW ENGINES CREATED: 0**, over the frozen estate: **practical problem representation** (evidence/provenance-REQUIRED — a problem without evidence is refused `EVIDENCE_REQUIRED`; evidence sanitized; confidence derived, never fabricated; external evidence never becomes authority — structural) · **three DISTINCT advisory scores** (PROBLEM / BUSINESS-OPPORTUNITY / EXPERIMENT — deterministic weighted composites, documented weights, factors exposed, UNKNOWN never zero) · **explainable problem LEVELS 0–4** (INTERESTING→ANNOYING→COSTLY→REVENUE_IMPACTING→MISSION_CRITICAL with reasons) · **bounded lifecycle** (OBSERVED→…→BUSINESS_CANDIDATE→BUILD_RECOMMENDED + REJECTED/DISMISSED/NEEDS_REVIEW — no idea→business jump) · **verified-payment-only revenue ladder** (INTEREST/WTP/EXPERIMENT_SUCCESS never reach REVENUE_VERIFIED — ONLY a verified payment does; 2→REPEAT_REVENUE, 3+→REPEATABLE_BUSINESS) · **zero/low-cost experiment planner** (NO_COST preferred; approvalRequired for spend/external actions — spending stays behind the existing authorization) · **customer discovery PREPARATION** (never a fabricated interview result) · **STOP / kill-bad-ideas** (deterministic evidence-driven reasons — the system CAN say "do not build this") · **advisory Business Candidate** (requires verified payment + WTP evidence) · **provider economics over the EXISTING Intelligence Fabric** (existing providers preferred; capability gap → CAPABILITY GAP DETECTED founder notification — NO automatic paid-provider adoption; PRIVATE never falls back to public) · **Opportunity Radar** in the Command Center (presentation-only) · gateway `world.*` +13 procedures (auth + rate tier + IDOR + zod) · owner-scoped problems store (in-memory + Postgres) · **`opportunity:benchmark` 20/20** wired into `npm run benchmarks` + vitest gate. Verification 2026-08-15: world-model **260/260 (21 files)**, services/api **1000+1 skip (50 files)**, web **218/218 (22 files)**, typecheck **0**, lint **0 errors · 0 warnings**, `next build` **PASS**, benchmarks all PASS (+20/20), coverage gate **8/8 touched** (world-model 91.21/82.14/92.33/94.2). Honest: **EMPTY datasets** — NO fabricated customers/revenue/market data; real observation entry ready; live world signals + real provider execution remain **OPERATOR-REQUIRED**; no real customer/revenue evidence exists) — [2026-08-15 report](./04_Sprints/SPRINT-038_COMPLETION_REPORT.md) · 🟢 **SPRINT-037 LIVE ORCHESTRATION & REAL-WORLD EXECUTION PROOF COMPLETE** (2026-08-15 — proves the CONFIGURE→PLAN→APPROVE→EXECUTE→VERIFY→MEASURE→RECORD loop over the frozen estate, **NEW ENGINES CREATED: 0**: **`OrchestrationPlanSource`** adapts an APPROVED `OrchestrationPlan` into a `FactoryCapabilityPlan` the EXISTING `ExecutionRunService` runs — approved-only structural gate, `executed:false` never flipped, capability mapping via the existing `CapabilityMapper`, honest provider-state mapping (UNKNOWN→CONFIGURE / AVAILABLE→READY), no alternate runtime · **`world.approveOrchestrationPlan`** routes exclusively through the existing Brain approval port (decision recorded; no voice/model/plan self-authorization) · **`world.startOrchestrationPlan`** composes the existing `ExecutionRunService.start` (auth + rate tier + IDOR + zod; unapproved plan → deterministic rejection; idempotent per plan) · Command Center automation view + AUTOMATION tab show the plan/provider/WHY/expected-vs-observed-cost/status/verification/outcome lifecycle (UNKNOWN stays UNKNOWN) · **`integration:provider` operator test** composes the REAL authorities, fails clearly without credentials (exit 2 — verified), strict cost/time limits, never falls back to fake adapters. Verification 2026-08-15: services/api **1000+1 skip (50 files)**, world-model **220/220 (18 files)**, typecheck **0**, lint **0/0**, `next build` **PASS**, benchmarks all PASS (16/16 + 13/13 + 11/11), coverage gate **45/45** (world-model 92.49/82.37/93.2/95.2; api 80.32 branch). Honest: LIVE provider execution remains **OPERATOR-REQUIRED** (production-config-check: AI PROVIDERS NOT_CONFIGURED — IMPLEMENTED + hermetic-TESTED, NOT LIVE-VERIFIED; an analytical workflow records an OPPORTUNITY, never REVENUE)) — [2026-08-15 report](./04_Sprints/SPRINT-037_COMPLETION_REPORT.md) · 🟢 **SPRINT-036 PRODUCTION MULTI-PROVIDER ORCHESTRATION COMPLETE** (2026-08-15 — bounded real workflows PLANNED across multiple providers, **NEW ENGINES CREATED: 0**, over the frozen estate: **`MultiProviderOrchestrator`** composition seam — per-step provider binding + WHY + expected cost via the existing fabric `selectStrategy` (CHEAP/FAST/QUALITY/PRIVATE/BALANCED; privacy overrides cost; PRIVATE + no local candidate → honest NO_SELECTION, never a public fallback) · **bounded deterministic retry/fallback policy** (never retries policy/cost/malformed; quota → fallback, no futile retry; transient → bounded retry ≤ 3 → privacy-safe fallback → STOP; verification disagreement → NEEDS_REVIEW, never price-resolved) · orchestration-plan store (owner-scoped, stable-key idempotent, in-memory + Postgres) · deterministic fixture scenario engine **11/11** · **`provider:benchmark`** (18th `benchmarks` harness, strategy tradeoff table) · gateway `world.orchestratePlan` + `world.listOrchestrationPlans` — plan `executed:false` + `authorizationRequired:true` STRUCTURAL; runtime path remains the existing execution bridge; provider output can never grant authority. Verification 2026-08-15: world-model **214/214**, gateway **987+1 skip**, typecheck **0**, lint **0/0**, `next build` **PASS**, benchmarks chain **18/18**, coverage gate **45/45** (world-model 92.49/82.72/92.95/95.19; api 80.32 branch). Honest: live multi-provider EXECUTION remains **OPERATOR-REQUIRED** — the normal suite is hermetic fixtures; autonomy levels unchanged) — [2026-08-15 report](./04_Sprints/SPRINT-036_COMPLETION_REPORT.md) · 🟢 **SPRINT-035 PRODUCTION HARDENING, CALIBRATION & FOUNDER COMMAND CENTER COMPLETION COMPLETE** (2026-08-15 — closes all six SPRINT-034 future items, **NEW ENGINES CREATED: 0**: **full coverage recompute** (world-model 93.73/83.92/95.60/96.50; services/api 93.18/**80.32**/95.14/93.98 — api branch restored 76.7%→80.32% via new `WorldBridgePorts.test.ts` 34 tests over the REAL gateway seams; **coverage gate 45/45 PASS**, no exclusions) · **Command Center drill-downs** (expandable WHAT/WHY/EVIDENCE/COST/RISK/NEXT-ACTION per item; approval detail through the existing Brain authority) · **bounded owner-scoped timeline** (composed from existing stores — no new event store; stable-key idempotent, paginated, owner-isolated) · **cost view** over the real CostLedger (OBSERVED/ESTIMATED/UNKNOWN — UNKNOWN never zero, ROI only with evidence) · **deterministic outcome/score calibration benchmark** (`CalibrationScenarios` + harness + vitest gate — **13/13 PASS**; `FEEDBACK_DELTA_MAX` 0.05 safety boundary proven intact; unverified evidence never scores; conflicting evidence visible; 17th `benchmarks` harness) · **voice presentation of the Command Center** (`CommandCenterQuestionRouter` + read-only `CommandCenterPresentationPort` — VOICE ≠ AUTHORIZATION preserved, no side effects) · **honest per-kind signal health** (lastSuccess/lastError; AVAILABLE only after a real observation) · **signal operator runbook** (no credentials) · **production configuration check** (`scripts/production-config-check.ts`). Verification 2026-08-15: world-model **200/200**, gateway **985+1 skip**, web **216/216**, voice **115/115**, typecheck **0**, lint **0 errors · 0 warnings**, `next build` **PASS**, benchmarks chain **17/17 PASS**, coverage gate **45/45**. Honest: Postgres, AI providers, world signals, STT/TTS, approved-blueprint execution and backup/recovery remain **OPERATOR-REQUIRED**) — [2026-08-15 report](./04_Sprints/SPRINT-035_COMPLETION_REPORT.md) · 🟢 **SPRINT-034 FOUNDER COMMAND CENTER & REAL-WORLD ACTIVATION COMPLETE** (2026-08-15 — closes all five SPRINT-033 gaps, **NEW ENGINES CREATED: 0**, over the frozen estate: **Founder Command Center** (presentation/composition ONLY — TODAY/PORTFOLIO/INTELLIGENCE/AUTOMATION/APPROVALS over the existing read models; approvals route solely through `world.decideBlueprintApproval` → the existing Brain approve/reject; no-spam TODAY, UNKNOWN-cost honesty, UNAVAILABLE signal honesty, boundary notice always present) · **Revenue → outcome feedback** (`OutcomeEvidence`: VERIFIED-only actuals — unverified/hypothesis/fabricated figures REFUSED, never inferred; bounded explainable feedback into `evaluateOpportunity`, Δ ≤ 0.05 per single outcome, every adjustment carries evidence) · **Live world-signal adapters** (`LiveSignalAdapter` over the frozen `WorldSignalSourcePort`: operator-configurable, provenance-required, untrusted-content sanitizer + payload caps + timeout, honest AVAILABLE/UNAVAILABLE/ERROR — never fabricated SUCCESS) · **Blueprint → approval-gated execution** (`BlueprintApprovalFactory`: requests only for C/D-gated steps re-classified through the existing ActionClassPolicy, `executed:false` structural, decisions exclusively through the Brain authority; execution stays with the existing bridge — no alternate path, no voice shortcut, no implicit approval) · **Cost-weighted revenue intelligence** (`CostWeightedRevenue` over CostLedger via a narrow `WorldCostPort`: margin/ROI-aware ranking, UNKNOWN cost/revenue/margin never zero, assumptions exposed) · durable owner-scoped stores (in-memory + Postgres `world_outcome_evidence`/`world_blueprint_approvals`) · gateway `world.*` +7 procedures (33 total — auth + rate tier + central IDOR + zod) · verification 2026-08-15 from source: world-model **187/187** (16 files, +45), gateway **951 passed · 1 skipped** (48 files), web **214/214** (22 files, +11 CommandCenter), typecheck **0**, lint **0**, `next build` **PASS** · honest: live world signals, live approved-blueprint execution and real revenue inflow remain **OPERATOR-REQUIRED**; no fabricated data, no income promises, no automatic business creation — the founder remains the ultimate authority) — [2026-08-15 report](./04_Sprints/SPRINT-034_COMPLETION_REPORT.md) · 🟢 **SPRINT-033 AUTONOMOUS COMPANY OS COMPLETE** (2026-08-15 — founder intelligence, revenue intelligence and the controlled execution blueprint over the SPRINT-032 world model, zero new engines: `RevenueIntelligence` evidence-carrying revenue streams + advisory snapshot + BUILD/BUY/AUTOMATE/OUTSOURCE/STOP/SCALE hints (evidence-only, never a promise) · `FounderBriefing` advisory no-spam composition (pending approvals / risks / revenue / cost / what-changed / signals) · `WorkflowExecutionBlueprint` — the controlled Opportunity → approval → workflow → selection → execution(existing bridge) → verification path as a REPRESENTATION (per-step A/B/C/D gates via the existing authority; executed:false + authorizationRequired:true structural; no voice-only authorization, no hidden execution, no autonomous spending) · Part B opportunity extensions (18 factors + closed category vocabulary) · gateway `world.*` 26 procedures · WorldPanel founder briefing + revenue cards; verification 2026-08-15: world-model 142/142, gateway 947+1 skip, web 203/203, typecheck 0, lint 0, next build PASS; honest: live world signals + live multi-provider execution operator-required, Founder Command Center FUTURE) — [2026-08-15 report](./04_Sprints/SPRINT-033_COMPLETION_REPORT.md) · 🟢 **SPRINT-032 WORLD MODEL & BUSINESS OPERATING SYSTEM COMPLETE** (2026-08-14 — the **bounded world representation + business operating model**, zero new engines: new `packages/world-model` — typed owner-scoped `WorldGraph` (23 entity types, closed 32-shape relation vocabulary, provenance-REQUIRED observations — no fabricated facts, stable-key idempotency, FIFO bounds, bounded queries) · configurable `BusinessUnit` · `OpportunityEconomics` (16-factor evidence-only scoring, zero/low-capital NO_COST/LOW_COST/CAPITAL_REQUIRED/UNKNOWN across ₹0–₹25k tiers, no income promises) · `AIWorkforce` (ROLE ≠ MODEL ≠ PROVIDER ≠ AGENT, advisory binding via the existing Fabric) · `WorkflowFactory` (generic business workflows + bounded decomposition through the existing Fabric bounds) · `HumanAIBoundary` (composes the existing A/B/C/D authority) · `WorldSignalSourcePort` interfaces ONLY (UNAVAILABLE/ERROR honesty) · gateway `world.*` 8 procedures (auth + rate tier + IDOR + zod) · durable owner-scoped in-memory + Postgres stores · `WorldPanel` in the AICompanion; verification: full suite 8 793 passed | 1 skipped (701 files), world-model 103/103 (99.3% stmts / 93.9% branches), typecheck 0, lint 0, next build PASS; honest: live world signals + live multi-provider execution OPERATOR-REQUIRED; the world model never approves/spends/executes, never promotes to memory) — [2026-08-14 report](./04_Sprints/SPRINT-032_COMPLETION_REPORT.md) · 🟢 **SPRINT-031 ACTIVE INTELLIGENCE & AUTONOMY CONTROL PLANE COMPLETE** (2026-08-14 — `packages/control-plane`: autonomy settings 0–5, emergency stop, cycle/gates, owner-scoped opportunity lifecycle; gateway `control.*` + `ControlPanel` UX) · 🟢 **SPRINT-030 AUTONOMOUS INTELLIGENCE, MULTI-PROVIDER ORCHESTRATION & CONTINUOUS OPERATIONS COMPLETE** (2026-08-14 — the **Intelligence Fabric**: an ADVISORY provider-neutral orchestration layer, zero new engines: `packages/intelligence-fabric` — `StrategyCandidate` orchestration contract · `SelectionStrategy` CHEAP/FAST/QUALITY/PRIVATE/BALANCED (deterministic, privacy-overrides-cost, PRIVATE + no local → honest no-selection) · `ProviderHealthLedger` (evidence-only — UNKNOWN until real calls observed, quota → UNAVAILABLE, never fabricated) · `CostPolicyGuard` (measure-only over the existing CostLedger — zero spend → undefined never 0; fail-closed caps task $1/daily $10/provider $5/workspace $20; execution budget stays the frozen RunBudgetGuard) · `WorkflowBounds` (depth 8/tasks 24/fan-out 8/calls 64/cost $5/600 s — no unbounded fan-out) · `VerificationChainPolicy` (bounded A→critique→verify; disagreement → NEEDS_REVIEW) · `ResultNormalizer` (provider-agnostic + secret redaction) · `AutonomyPolicy` (levels 0–5 over the existing A/B/C/D; class B needs an explicit user-authorization record; class C only ASKS; class D never) · gateway `fabric.*` 8 procedures (auth + rate tier + IDOR + zod) · cadence optionally refreshes proactive recommendations (no autonomous action) · `FabricPanel` Provider Network in the AICompanion; verification: full suite 8 613 passed | 1 skipped (682 files), typecheck 0, lint 0, coverage gate 43/43, next build PASS, benchmarks EXIT 0; honest: the Fabric observes/measures/selects/validates — never executes/spends/authorizes; live multi-provider execution OPERATOR-REQUIRED) — [2026-08-14 report](./04_Sprints/SPRINT-030_COMPLETION_REPORT.md) · 🟢 **SPRINT-029 PROACTIVE INTELLIGENCE & AUTOMATION FABRIC COMPLETE** (2026-08-14 — proactive intelligence as a COMPOSITION layer, zero new engines: new `packages/proactive` — evidence-only structured recommendations (10 categories, estimates never fabricated), `ProactiveIntelligenceService` over the existing Brain pipeline (stable-key idempotency, dismissed never resurrected), `ActionClassPolicy` A/B/C/D composing the frozen `SENSITIVE_ACTIONS` (accept on class C returns APPROVAL_REQUIRED — the layer can never authorize), `AutomationDiscovery` (≥2-occurrence evidence floor, full workflow representation), `BusinessOpportunityAssessor` (research/score only, never spends/commits), no-spam `DailyBriefingAssembler`; owner-scoped store (in-memory + Postgres) in the shared persistence bundle; gateway `proactive.*` 6 procedures (auth + rate tier + IDOR + zod); `ProactivePanel` UX in the AICompanion (WHAT/WHY/VALUE/RISK/COST/ACTION, approval posture, durable dismiss, a11y); canonical `05_Docs/CURRENT_ARCHITECTURE_STATE.md`; verification re-run: full suite 8 540 passed | 1 skipped (671 files), typecheck 0, lint 0, coverage gate 42/42, next build PASS, benchmarks EXIT 0; honest: background cadence prepared-not-productized (SPRINT-030+), outcome-memory evidence honest-empty, live discovery/execution operator steps) — [2026-08-14 report](./04_Sprints/SPRINT-029_COMPLETION_REPORT.md) · 🟢 **SPRINT-028 VEDMOULYA VOICE ASSISTANT COMPLETE** (2026-08-13 — production voice experience over the existing Brain, zero new engines: real runtime-backed STT/TTS adapters (provider-neutral, bounded payloads, AbortSignal + timeouts, credentials server-side); Voice→Brain bridge (ANSWER→ai.stream Q&A runtime, ACTION→brain.createTask); VOICE ≠ AUTHORIZATION enforced + proven (sensitive actions need the non-voice confirm button → existing Brain approval); owner-scoped conversation turns with no promotion into facts/preferences/outcomes/learning; voice.status truth via live probe (CONFIGURED/UNAVAILABLE/ERROR/MOCK, never fabricated); unified VoicePanel UX in the AICompanion (9 states, mic, transcript, playback, cancel, retry, permission recovery, a11y); full suite 8 467 passed | 1 skipped (662 files), typecheck 0, lint 0, coverage gate 41/41, next build PASS; honest: real STT/TTS providers operator-required) — [2026-08-13 report](./04_Sprints/SPRINT-028_COMPLETION_REPORT.md) · 🟢 **SPRINT-027 PLATFORM INTEGRITY & SPEECH FOUNDATION COMPLETE** (2026-08-13 — integrity gaps closed + speech seams in place, zero new engines: R-1 rate limiting → async RateLimiter port (in-memory honest default + explicit Redis backend, loud explicit degradation); R-2 gateway audit durable + owner-scoped via new AuditLogStore; new `packages/voice` workspace (SpeechToTextPort/TextToSpeechPort seams + mock adapters, VoiceIntentGate VOICE ≠ AUTHORIZATION, owner-scoped bounded conversation store); dead `services/notifications` deleted (proven 0 refs); dead Mic removed + Phoenix → VedMoulya branding; pre-existing P1 fixed — `next build` was red on main, now PASS; full suite 8 100/8 100 PASS (646 files), typecheck 0, lint 0, benchmarks GREEN (30/30 + 36/36 + 25/25); honest: real STT/TTS operator-required, voice.status reports MOCK never CONFIGURED) — [2026-08-13 report](./04_Sprints/SPRINT-027_COMPLETION_REPORT.md) · 🟢 **SPRINT-026 VOICE INTELLIGENCE + COMPLETE-SYSTEM ARCHITECTURE AUDIT COMPLETE** (2026-08-13 — full 16-phase audit + architecture decision; system verified coherent (spot-checked 251/251 core tests, typecheck 0); voice decided as an interaction layer over the existing Brain — only missing foundation is STT/TTS runtime adapters + a conversation store, with the safety rule VOICE ≠ AUTHORIZATION; proactive/automation compose existing engines, no autonomous-agent engine; two P1 operational gaps documented (in-memory rate limit, in-memory gateway audit); 10 deliverables in `04_Sprints/SPRINT-026_*`; roadmap = 4 sprints: SPRINT-027 integrity+speech foundation → SPRINT-028 voice → SPRINT-029 proactive+automation → SPRINT-030 production readiness) — [2026-08-13 report](./04_Sprints/SPRINT-026_COMPLETION_REPORT.md)
- **Frozen contracts:** [Version Manifest](./03_Architecture/VEDMOULYA_V1_VERSION_MANIFEST.md) · [Platform Contract](./03_Architecture/VEDMOULYA_PLATFORM_CONTRACT.md) · [Architecture Freeze](./03_Architecture/ARCHITECTURE_FREEZE.md) · [API v1](./03_Architecture/API_V1_CONTRACT.md) · [Database v1](./03_Architecture/DATABASE_V1.md) · [Environment v1](./07_Operations/ENVIRONMENT_V1.md)
- **Next phase:** EPIC-006 — VEDMOULYA APPLICATION PLATFORM (LOOP-ENGINE done; APP-FACTORY done; **APP-WORKSPACE done (EPIC-008, 2026-08-09)**; **REQUIREMENTS-ENGINE done (EPIC-009, 2026-08-09)**; **APP-VISUAL done (EPIC-010, 2026-08-09)**; **PROD-VALIDATION done (EPIC-011, 2026-08-09)**; **OBSERVABILITY & CONTROL PLANE done (EPIC-012, 2026-08-10)**; **PROVIDER EXPERIENCE done (EPIC-012A, 2026-08-10)**; **PROVIDER DISCOVERY done (EPIC-012B, 2026-08-10)**; APP-002 Agent Builder next)

---

## Overview

VedMoulya is a monorepo platform (Execution Operating System) composed of:

- **3 application layers** — Next.js web app, API gateway (tRPC), shared UI component library
- **12 services** — identity, knowledge, memory, decision, execution, orchestrator (AI), learning, marketplace, notifications, career, business, and the API gateway
- **10 packages** — core, domain, services, ai, ui, config, information, intelligence, shared, testing
- **TypeScript throughout** with strict mode, Vitest unit testing, ESLint, Prettier

The platform is architected around five core engines: **Identity**, **Knowledge Graph**, **Memory**, **Decision Intelligence**, and **Execution Intelligence**, unified by an **AI Orchestrator** with pluggable providers (OpenAI, Anthropic, Mock) and a Life OS **Dashboard**.

## Architecture

```
apps/web                 Next.js 15 web application (React 19, tRPC client, Tailwind CSS)
apps/*                   Additional applications
packages/core            Foundation: DI, event bus, config, logging, metrics, tracing, health
packages/domain          Domain entities, factories, value objects
packages/services        Application services (dashboard, career, learning, business, AI orchestration)
packages/ai              AI domain types (requests, responses, capabilities, providers)
packages/ui              UI component library (Radix UI + Tailwind)
packages/context-fabric   Post-V1 Context & Personal Intelligence Fabric (APP-001): personal/business
                         intelligence graphs, permission-gated hybrid retrieval, provenance,
                         explanations, minimum-useful context packages (`contextFabric.*` API)
packages/loop-engine      Orchestrated AI / Loop Engine (EPIC-006): typed GoalSpecification +
                         TaskGraph, bounded loop (6 budgets), deterministic critic, evidence-first
                         adaptive refinement, explainable trace, explicit termination, `loop.*` API
packages/app-factory      AI Application Factory (EPIC-007): the application layer above the frozen
                         platform — spec/architecture/plan engines, Phase 8 approval gate, controlled
                         file ops + execution policy, validation/security/UI gates, economics,
                         isolated workspaces, deploy + VCS abstraction, `factory.*` API, `/applications`
packages/*               config, information, intelligence, shared, testing
services/api             API gateway — tRPC routers + middleware (auth, audit, rate-limit)
services/identity        Authentication: JWT, sessions, Google OAuth
services/orchestrator    AI provider abstraction, fallback/retry, request cache, metrics
services/knowledge       Knowledge graph service
services/memory          Memory engine service
services/decision        Decision intelligence service
services/execution       Execution engine service
services/*               learning, marketplace, notifications, career, business
tests/                   Shared vitest setup (provisions required env vars)
```

### Key engineering conventions

- **Fail-fast configuration** — production secrets (`AUTH_JWT_SECRET`, `IDENTITY_DATABASE_URL`, `REDIS_URL`, AI keys, OAuth/SMTP credentials) are validated at startup; missing, empty, placeholder, or localhost values are rejected with clear messages outside `NODE_ENV=development`.
- **Clean architecture** — services depend on packages, never the reverse; domain layer is framework-free.
- **Workspace tooling** — every workspace with tests ships a `vitest.config.ts`, `test` script, and coverage config.

## Quick Start

```bash
# Prerequisites: Node.js >= 20, npm >= 10, Docker (optional for infra)

git clone https://github.com/ak9848515-dev/vedmoulya.git
cd VedMoulya
npm install
npm run build:core    # Build foundational packages
```

### Configure environment

```bash
cp .env.example .env.local
```

Edit `.env.local`:

- Set `AUTH_JWT_SECRET` (required — generate with `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`)
- Set `IDENTITY_DATABASE_URL` and `REDIS_URL` (required outside `NODE_ENV=development`)
- Set AI provider keys if you use AI features

### Run

```bash
npm run doctor        # One-shot startup diagnostics (npm run doctor:prod for production)
npm run preflight     # Startup preflight (npm run preflight -- --mode production)
npm run dev           # Start the web app (Next.js dev server)
npm run verify        # Bounded, ANSI-free verification (tests + typecheck + lint + doctor)
npm run test          # Run all unit tests (Vitest workspace)
npm run build         # Build all workspaces
```

Startup is deterministic and self-diagnosing (EPIC-018 + EPIC-019):
`scripts/startup.sh` (production) and `scripts/startup.sh --dev` run the
preflight first — every failure answers WHAT / WHY / REQUIRED / CONTINUES /
ACTION (see [EPIC-018 PREFLIGHT](./09_Documents/EPIC_018_PREFLIGHT.md)). The
flow is ENV CHECK → DEPENDENCY CHECK (Docker) → DATABASE/REDIS/AI CHECK
(preflight) → BUILD CHECK (production) → PORT CHECK → SERVER START → bounded
HEALTH CHECK. Port conflicts are never silent: `startup.sh` detects an occupied
port, names the owner PID and prompts ([1] stop [2] another port [3] cancel),
or fails deterministically with `--ci`. Bounded runs use `--timeout <seconds>`.
`npm run doctor` reports the same truth as a PASS/FAIL table (Node, npm, TS
runtime, database, redis, Docker, build, AI runtime, default provider, provider
adapters, port, configuration) — never printing secrets.

Development runs without Docker/AI keys using the in-memory convention and the
deterministic mock provider; production requires Docker Postgres/Redis, a web
build (`npm run build -w apps/web`), `AUTH_JWT_SECRET` and real AI configuration
(OpenAI/DeepSeek key, or an explicit `AI_ENABLE_MOCK=true`) — production never
silently falls back to mock. Provider state is truthful everywhere: OpenAI and
DeepSeek are runtime-executable; Anthropic, Google, OpenRouter and Ollama are
catalog-only (`UNSUPPORTED_RUNTIME` — see
[EPIC-019 PROVIDER RUNTIME MATRIX](./09_Documents/EPIC_019_PROVIDER_RUNTIME_MATRIX.md)).

## Development

- **Workspace structure:** `apps/*`, `packages/*`, `services/*` — managed via npm workspaces.
- **Scripts (root):** `npm run dev`, `npm run preflight`, `npm run doctor`, `npm run verify`, `npm run check-port`, `npm run build`, `npm run test`, `npm run lint`, `npm run typecheck`, `npm run format`.
- **Service dev servers:** each service has `dev` (tsx watch) and `build` (tsc) scripts.
- **Adding a workspace:** create `package.json` with the `@vedmoulya/*` scope, wire into `vitest.workspace.ts` if it has tests.
- **Conventional commits** enforced via commitlint + husky; commit type/scope from the repo's `commitlint.config.ts`.

## Testing

```bash
npm run test              # All unit tests (Vitest workspace — 500+ files / 6 700+ tests)
npm run test:coverage     # Coverage report (per-workspace v8 coverage config)
npm run test:e2e          # Playwright end-to-end (apps/web)
npm run test:a11y         # Accessibility audit
npm run test:performance  # Bundle size checks
```

- Unit tests use Vitest with a shared setup (`tests/vitest.setup.ts`) that provisions required environment variables before any module imports `@vedmoulya/core`.
- The workspace runner is defined in `vitest.workspace.ts` (`packages/*/vitest.config.ts`, `services/*/vitest.config.ts`).

## Build

```bash
npm run build         # Build every workspace (tsc / next build)
npm run build:core    # Build foundational packages first
npm run clean:build   # Clear Next.js build cache
```

- Deterministic builds; `*.tsbuildinfo` artifacts are gitignored.
- CI runs typecheck → lint → tests → build → e2e in `.github/workflows/ci.yml`.

## Deployment

- **Containerized services** via `docker-compose.yml` (PostgreSQL, Redis, service containers).
- **Environment:** configure all secrets in the runtime environment (see `.env.example`). Startup fails fast with a clear message if a required production secret is missing, empty, placeholder, or a localhost default.
- **CI/CD:** GitHub Actions workflow runs 10 quality gates (architecture, code quality, testing/coverage, accessibility, performance, security scan, build, e2e).
- **Release process:** conventional commits → tag `v1.0.0` (current release) → certification reports under `docs/`.

## Documentation

- [Project report (2026-07-31)](./docs/PROJECT_REPORT_2026-07-31.md)
- [Release notes draft](./docs/RC-001_D18_Release_Notes_Draft.md)
- [Architecture standards](./09_Documents/Architecture%20Standards.md)
- [Implementation strategy](./06_Implementation/01_Implementation_Strategy.md)
- [Mission tracker](./09_Documents/Mission%20Tracker.md)
